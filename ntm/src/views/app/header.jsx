import React from 'react';
import { Navbar } from 'react-bootstrap';

export default React.memo(() => {
  return (
    <Navbar bg="dark">
      <Navbar.Brand href="#">
      </Navbar.Brand>
    </Navbar>
  )
});
